// screen that displays the contents of the adoption request
import { 
    SafeAreaView,
    ScrollView,
    Text
} from "react-native"

import FlexibleTextInput from "../../components/admin/FlexibleTextnput"


const AdoptionRequestForm = () =>{
    return (
        <SafeAreaView>
            <ScrollView>

            </ScrollView>
        </SafeAreaView>
    )
}